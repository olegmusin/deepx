import React from 'react';

import { GamesList } from './components';
import './App.css';

const App = () => {
    return (
        <div>
            <h1>React CRUD with global store on hooks</h1>
            <GamesList />
        </div>
    );
};

export default App;
